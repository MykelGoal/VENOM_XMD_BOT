import { makeBotToggle } from "../_shared/bottoggle";

export default makeBotToggle({
  name: "antidelete",
  key: "antidelete",
  label: "Anti-delete (recover deleted msgs)",
  description: "Toggle anti-delete (recover deleted msgs).",
});
