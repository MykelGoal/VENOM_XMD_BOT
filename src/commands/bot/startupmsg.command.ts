import { makeBotToggle } from "../_shared/bottoggle";

export default makeBotToggle({
  name: "startupmsg",
  key: "startupmsg",
  label: "Startup message to owner",
  description: "Toggle startup message to owner.",
});
