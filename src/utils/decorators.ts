/**
 * Optional command decorators.
 *
 * This project uses plain object-based commands (see types/command.type.ts),
 * which keeps things simple and framework-free. If you later prefer a
 * class-based command style, you can implement decorators here, e.g.:
 *
 *   @cmd({ name: 'ping', category: 'general' })
 *   class PingCommand { run(ctx) { ... } }
 *
 * Left intentionally minimal to avoid over-engineering.
 */
export {};
