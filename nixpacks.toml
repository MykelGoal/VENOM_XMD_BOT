# Nixpacks build config (Railway, Koyeb, and other Nixpacks-based hosts).
# Guarantees Node 20 + ffmpeg are available at build/runtime.

[phases.setup]
nixPkgs = ["nodejs_20", "ffmpeg"]

[phases.build]
cmds = ["npm install", "npm run build"]

[start]
cmd = "npm start"
