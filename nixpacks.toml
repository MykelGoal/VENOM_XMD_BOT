# Nixpacks build config (Railway, Koyeb, and other Nixpacks-based hosts).
# Guarantees Node 24 + ffmpeg are available at build/runtime.

[phases.setup]
nixPkgs = ["nodejs_24", "ffmpeg"]

[phases.build]
cmds = ["npm ci", "npm run build"]

[start]
cmd = "npm start"
